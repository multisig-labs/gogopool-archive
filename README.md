# GoGoGadget GoGoPool! 

## First time setup
```
npm install
brew install just
curl -L https://foundry.paradigm.xyz | bash (from https://github.com/foundry-rs/foundry)
foundryup
forge install
just build
just test
```

